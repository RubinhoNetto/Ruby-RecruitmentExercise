<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= !@organization.nil? ? @organization.name : 'Back-end Test' %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <div class="container">
      <div class="navbar">
        <h1 class="navbar-brand"><%= !@organization.nil? ? @organization.name : 'Back-end Test' %></h1>
        <%= button_to 'Fetch Data', { action: "fetch_data" }, { method: :get, class: 'btn btn-outline-dark' }%>
      </div>

      <div class="container-fluid">
        <%= yield %>
      </div>
    </div>
  </body>
</html>

